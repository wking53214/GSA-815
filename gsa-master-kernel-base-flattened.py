""" Program Name: GovernanceSystemsArchitectureMasterKernel Description: Integrated multi-tenant zero-trust validation architecture and                deterministic policy runtime middleware pipeline designed to govern,                sanitize, and track generative inference operations. """ from __future__ import annotations import asyncio import functools import hashlib import hmac import json import logging import re import secrets import time from dataclasses import asdict, dataclass from datetime import datetime from typing import Any, Awaitable, Callable, Dict, Final, List, Optional, Set, Tuple from fastapi import Depends, FastAPI, HTTPException, Request, Security, status from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials from pydantic import BaseModel, Field from starlette.middleware.base import BaseHTTPMiddleware # ===================================================================== # SYSTEM-WIDE OPERATIONAL CONSTANTS # ===================================================================== logging.basicConfig(     level=logging.INFO,     format="%(asctime)s - GSA_KERNEL - %(levelname)s - %(message)s" ) logger = logging.getLogger("GovernanceSystemsArchitectureMasterKernel") DEFAULT_BUDGET_MS: Final[float] = 42.0 MAX_RISK_THRESHOLD: Final[float] = 0.80 LONG_PAYLOAD_THRESHOLD: Final[int] = 500 EPOCH_WINDOW_SECONDS: Final[int] = 60 SIMULATED_TELEMETRY_DELAY: Final[float] = 0.005 IDENTITY_WEIGHT: Final[float] = 0.15 COURTESY_WEIGHT: Final[float] = 0.10 LONG_PAYLOAD_WEIGHT: Final[float] = 0.20 CENTRAL_REGULAR_EXPRESSION_CACHE: Dict[str, re.Pattern] = {     "pronominal_purge": re.compile(r"\b(i|me|my|mine|myself|we|us|our|ourselves|ours)\b", re.IGNORECASE),     "syntactic_breach": re.compile(r"\b(may|might|could|seems|generally|potentially|likely|perhaps|maybe)\b", re.IGNORECASE),     "prohibited_abstract_verbs": re.compile(r"\b(improve|optimize|enhance|enable|support|strengthen|utilize|leverage)\b", re.IGNORECASE),     "causal_link": re.compile(r"\b(because|due to|driven by|resulting from|caused by)\b", re.IGNORECASE),     "metric_verification": re.compile(r"\b\d+(\.\d+)?%|\b\d+\b"),     "system_keyword": re.compile(r"system", re.IGNORECASE) } HIGH_RISK_TOKENS: Set[str] = {     "bypass", "override", "root", "admin", "jailbreak", "ignore",      "instructions", "constitution", "gatekeeper", "exploit",      "vulnerability", "inject", "malicious", "purge" } # ===================================================================== # SYSTEM METRIC STATE SUBSTRATE # ===================================================================== class InfrastructureStateSubstrate:     """Maintains state metrics, system limits, and entropy drift limits."""     def __init__(self):         self.emergency_tier: int = 0         self.system_health_index: float = 1.0         self.sustainability_score: float = 1.0         self.current_trajectory: Dict[str, float] = {"Resource_Scarcity": 0.1, "Logic_Entropy": 0.02}         self.resource_containment_active: bool = False         self.integrity_debt_index: float = 0.0 GLOBAL_SYSTEM_SUBSTRATE = InfrastructureStateSubstrate() # ===================================================================== # DATA CAPSULE DEFINITIONS (TYPE-SAFE PRIMITIVES) # ===================================================================== @dataclass(frozen=True) class ValidationRuleResult:     passed: bool     rule_identifier: str     error_details: Optional[str] = None @dataclass(frozen=True) class PolicyEvaluationResult:     allowed: bool     status_code: str     risk_score: float @dataclass(frozen=True) class OperationalTelemetry:     budget_ms: float     entropy: float     risk_score: float @dataclass(frozen=True) class AuditLedgerBlock:     index: int     timestamp: float     proof_value: int     record_metadata: str     previous_block_hash: str     current_block_hash: str @dataclass class ObservationCapsule:     request_text: str     raw_request_payload: Dict[str, Any]     structured_messages: List[Dict[str, Any]]     context_metadata: Dict[str, Any]     audit_tracking_enabled: bool = True @dataclass class ThreatProfileCapsule:     high_risk_token_matches: List[Dict[str, Any]]     system_segment_matches: List[Dict[str, Any]]     calculated_risk_score: float @dataclass class GovernanceDecisionCapsule:     allowed: bool     rejection_reason: str     operational_regime: str @dataclass class ExecutionResultCapsule:     status_code: int     session_identifier: str     response_payload: str     telemetry_data: dict     forensic_signature: str     authorization_tag: str     total_runtime_ms: float @dataclass class ExplanationPackageCapsule:     generation_timestamp: str     decision_matrix: Dict[str, Any]     structural_parity_index: float @dataclass class AuditRecordCapsule:     trace_identifier: str     generation_timestamp: str     ledger_index: int     payload_hash_signature: str @dataclass class AdaptationDirectiveCapsule:     patch_applied: bool     generation_timestamp: str @dataclass class GovernanceTraceBundle:     observation: ObservationCapsule     threat_profile: ThreatProfileCapsule     decision: GovernanceDecisionCapsule     execution_result: Optional[ExecutionResultCapsule]     explanation: ExplanationPackageCapsule     audit_record: AuditRecordCapsule     adaptation_directive: Optional[AdaptationDirectiveCapsule] # ===================================================================== # MULTI-TENANCY INTERFACE MODEL SCHEMA (FASTAPI CONTRACTS) # ===================================================================== class MetricsPayload(BaseModel):     compute_draw_coefficient: float = Field(..., description="Hardware core utility boundaries.")     resource_draw_coefficient: float = Field(..., description="System power footprints.")     logic_entropy_coefficient: float = Field(default=0.02, description="Logical trace runtime degradation variable.") class TouchpointChangePayload(BaseModel):     alteration_identifier: str     target_component_name: str     ruleset_delta_map: Dict[str, Any] class EvaluationRequest(BaseModel):     request_text: str = Field(..., description="Raw text parameter tracking to target inference gateway.")     metrics: MetricsPayload = Field(..., description="Hardware state dimensions array.")     modifications: Optional[List[TouchpointChangePayload]] = Field(default_factory=list)     messages: Optional[List[Dict[str, Any]]] = Field(default_factory=list) # ===================================================================== # EXECUTION ROUTING PROTECTION DECORATORS # ===================================================================== class GovernanceViolation(Exception):     """Exception raised when an evaluation bundle breaches critical invariant constraints."""     pass def requires_approval(func: Callable) -> Callable:     """Gates access to a target routine by validating internal trace approvals."""     @functools.wraps(func)     def wrapper(bundle: GovernanceTraceBundle, *args, **kwargs) -> Any:         decision = getattr(bundle, "decision", None)         if decision is range or not decision.allowed:             raise GovernanceViolation("Operational Access Denied: Bound execution blocked by GSA ruleset.")         return func(bundle, *args, **kwargs)     return wrapper # ===================================================================== # INTAKE FENCING ENGINE (LAYER 0 & LAYER 1) # ===================================================================== class ThreatAnalysisEngine:     """Pillar 2: Analyze. Compiles structural analytics across raw ingested frames."""     def evaluate(self, observation: ObservationCapsule, baseline_risk_score: float) -> ThreatProfileCapsule:         token_matches: List[Dict[str, Any]] = []         segment_matches: List[Dict[str, Any]] = []         lines = observation.request_text.split("\n")                      for index, line in enumerate(lines, start=1):             for token in line.split():                 clean_token = token.strip(".,;:!?\"'").lower()                 if clean_token in HIGH_RISK_TOKENS:                     token_matches.append({"token": clean_token, "line_number": index})             if CENTRAL_REGULAR_EXPRESSION_CACHE["system_keyword"].search(line):                 segment_matches.append({"line_number": index, "text_segment": line})                              adjusted_risk = max(baseline_risk_score, 0.95 if len(token_matches) > 0 else baseline_risk_score)         return ThreatProfileCapsule(             high_risk_token_matches=token_matches,             system_segment_matches=segment_matches,             calculated_risk_score=adjusted_risk         ) class DeterministicPolicyRuntime:     """Executes fast zero-trust structural indexing and validation cryptography over telemetry."""     def __init__(self, key_material_bytes: bytes):         self._secret_signing_key: bytes = key_material_bytes     def evaluate_policy(self, input_string: str) -> PolicyEvaluationResult:         if not input_string or not input_string.strip():             return PolicyEvaluationResult(allowed=False, status_code="EMPTY_INPUT_VAL", risk_score=1.0)                      calculated_risk = 0.0         if CENTRAL_REGULAR_EXPRESSION_CACHE["pronominal_purge"].search(input_string):              calculated_risk += IDENTITY_WEIGHT         if CENTRAL_REGULAR_EXPRESSION_CACHE["syntactic_breach"].search(input_string):              calculated_risk += COURTESY_WEIGHT         if len(input_string) > LONG_PAYLOAD_THRESHOLD:              calculated_risk += LONG_PAYLOAD_WEIGHT                          calculated_risk = round(calculated_risk, 4)         if calculated_risk >= MAX_RISK_THRESHOLD:             return PolicyEvaluationResult(allowed=False, status_code="RISK_THRESHOLD_EXCEEDED", risk_score=calculated_risk)         return PolicyEvaluationResult(allowed=True, status_code="SUCCESS_PASS", risk_score=calculated_risk)     async def generate_telemetry(self, input_string: str, risk_score: float) -> OperationalTelemetry:         string_length = len(input_string)         entropy = round(1.0 + (string_length * 0.002), 4)         budget_ms = round(DEFAULT_BUDGET_MS * (1.0 / max(entropy, 1.0)), 4)         await asyncio.sleep(SIMULATED_TELEMETRY_DELAY)         return OperationalTelemetry(budget_ms=budget_ms, entropy=entropy, risk_score=risk_score)     def generate_forensic_signature(self, payload_string: str, telemetry: OperationalTelemetry) -> str:         temporal_window_bucket = int(time.time() // EPOCH_WINDOW_SECONDS)         composite_data = f"{payload_string}|{telemetry.entropy}|{telemetry.risk_score}|{temporal_window_bucket}"         return hashlib.sha256(composite_data.encode("utf-8")).hexdigest()     def generate_auth_tag(self, forensic_signature_string: str) -> str:         return hmac.new(self._secret_signing_key, forensic_signature_string.encode("utf-8"), hashlib.sha256).hexdigest() # ===================================================================== # CORE CONSTITUTIONAL BOUNDARY PROTECTION GATES # ===================================================================== class BoundaryGate:     """Pillar 3: Govern. Evaluates metrics frames against active state policies."""     def __init__(self):         self._validation_rules: List[Callable[[ObservationCapsule], ValidationRuleResult]] = [             lambda ctx: ValidationRuleResult(passed="harm" not in ctx.request_text.lower(), rule_identifier="no_harmful_requests", error_details="Harm sequence signature matched."),             lambda ctx: ValidationRuleResult(passed=len(ctx.request_text) < 5000, rule_identifier="within_capability", error_details="Payload frame allocation bounds spilled."),             lambda ctx: ValidationRuleResult(passed=GLOBAL_SYSTEM_SUBSTRATE.system_health_index >= 0.50, rule_identifier="substrate_viability", error_details="Substrate degradation threshold broken.")         ]     def evaluate(self, observation: ObservationCapsule, threat: ThreatProfileCapsule) -> GovernanceDecisionCapsule:         for rule in self._validation_rules:             result = rule(observation)             if not result.passed:                 return GovernanceDecisionCapsule(allowed=False, rejection_reason=result.error_details or "Boundary pass error", operational_regime="emergency")                      if threat.calculated_risk_score >= MAX_RISK_THRESHOLD:             return GovernanceDecisionCapsule(allowed=False, rejection_reason="Threat index threshold exceeded", operational_regime="emergency")         return GovernanceDecisionCapsule(allowed=True, rejection_reason="All invariant verification passes cleared", operational_regime="stable") # ===================================================================== # MODEL INTERCEPTION COMPLIANCE TIERS (CLOSED-LOOP RUNTIME GATES) # ===================================================================== class IdentityGate:     def validate(self, text: str) -> bool:          return not bool(CENTRAL_REGULAR_EXPRESSION_CACHE["pronominal_purge"].search(text)) class HedgingGate:     def validate(self, text: str) -> bool:          return not bool(CENTRAL_REGULAR_EXPRESSION_CACHE["syntactic_breach"].search(text)) class CausalityGate:     def validate(self, text: str) -> bool:         return (bool(CENTRAL_REGULAR_EXPRESSION_CACHE["causal_link"].search(text)) or                  bool(CENTRAL_REGULAR_EXPRESSION_CACHE["metric_verification"].search(text))) class StructureNormalizer:     def normalize(self, text: str) -> str:          return CENTRAL_REGULAR_EXPRESSION_CACHE["prohibited_abstract_verbs"].sub("use", text) class KineticGovernor:     def __init__(self, target_latency_ms: float = 15.0):         self._target_delay_seconds: float = target_latency_ms / 1000.0         self._pacing_coefficient: float = 0.815     async def calculate_temporal_budget(self, payload_text: str) -> float:         calculated_delay = (len(payload_text.split()) * 0.002) * self._pacing_coefficient         return max(self._target_delay_seconds, min(calculated_delay, 0.200))     async def apply_liturgical_pause(self, delay_duration_seconds: float) -> None:          await asyncio.sleep(delay_duration_seconds) # ===================================================================== # INTERCEPTOR EXECUTION RUNTIME PIPELINE # ===================================================================== class ExecutionPipeline:     """Pillar 4: Execute. Guides inference paths through structural validation loops."""     def __init__(self, dpr_runtime: DeterministicPolicyRuntime):         self._normalizer: StructureNormalizer = StructureNormalizer()         self._identity_gate: IdentityGate = IdentityGate()         self._hedging_gate: HedgingGate = HedgingGate()         self._causality_gate: CausalityGate = CausalityGate()         self._governor: KineticGovernor = KineticGovernor()         self._dpr: DeterministicPolicyRuntime = dpr_runtime         self._processed_output_cache: Set[str] = set()     async def run(self, observation: ObservationCapsule, decision: GovernanceDecisionCapsule, generator_routine: Callable[[str], Awaitable[str]], maximum_retry_bounds: int = 3) -> ExecutionResultCapsule:         start_timestamp = time.perf_counter()         working_prompt = observation.request_text                      for attempt in range(1, maximum_retry_bounds + 1):             raw_response = await generator_routine(working_prompt)             clean_response = self._normalizer.normalize(raw_response)                              identity_pass = self._identity_gate.validate(clean_response)             hedging_pass = self._hedging_gate.validate(clean_response)             causality_pass = self._causality_gate.validate(clean_response)                              response_hash = hashlib.md5(clean_response.encode("utf-8")).hexdigest()             pattern_looping_detected = response_hash in self._processed_output_cache                              if identity_pass and hedging_pass and causality_pass and not pattern_looping_detected:                 self._processed_output_cache.add(response_hash)                 pacing_delay = await self._governor.calculate_temporal_budget(clean_response)                 await self._governor.apply_liturgical_pause(pacing_delay)                                      telemetry = await self._dpr.generate_telemetry(clean_response, 0.0)                 forensic_sig = self._dpr.generate_forensic_signature(clean_response, telemetry)                 auth_tag = self._dpr.generate_auth_tag(forensic_sig)                                      total_runtime = round((time.perf_counter() - start_timestamp) * 1000, 4)                 return ExecutionResultCapsule(                     status_code=200, session_identifier=f"GSA-LOOP-{secrets.token_hex(2).upper()}",                     response_payload=clean_response, telemetry_data=asdict(telemetry),                     forensic_signature=forensic_sig, authorization_tag=auth_tag,                     total_runtime_ms=total_runtime                 )                                  self._processed_output_cache.add(response_hash)             failure_reasons: List[str] = []             if not identity_pass: failure_reasons.append("Identity containment breakdown")             if not hedging_pass: failure_reasons.append("Hedging string trace parsed")             if not causality_pass: failure_reasons.append("Causal baseline verification failure")             if pattern_looping_detected: failure_reasons.append("Generative structure loop triggered")                              working_prompt = (f"{observation.request_text}\n[INSTRUCTIONAL_DELTA]: Compliance constraints mapping "                               f"failed due to: {', '.join(failure_reasons)}. Re-render output text payload with absolute structural density.")                          raise SystemError("GSA_PIPELINE_COLLAPSE: Closed-loop policy reconciliation could not attain convergence bounds.") # ===================================================================== # FORENSICS, TELEMETRY LEDGERS, AND EVENT PERSISTENCE STORES # ===================================================================== class ExplanationEngine:     """Pillar 5: Explain. Emits diagnostics metrics detailing runtime policy paths."""     def build(self, observation: ObservationCapsule, threat: ThreatProfileCapsule, decision: GovernanceDecisionCapsule, execution_result: Optional[ExecutionResultCapsule]) -> ExplanationPackageCapsule:         matrix = {"input_risk_score": threat.calculated_risk_score, "governance_regime": decision.operational_regime, "allowed": decision.allowed}         if execution_result: matrix["runtime_telemetry"] = execution_result.telemetry_data         return ExplanationPackageCapsule(generation_timestamp=datetime.utcnow().isoformat(), decision_matrix=matrix, structural_parity_index=1.0000) class AuditLedger:     """Pillar 6: Audit. Commits trace nodes down onto a crypto-linked blockchain ledger."""     def __init__(self):         self.blockchain_ledger: List[AuditLedgerBlock] = []         self._generate_genesis_block()     def _generate_genesis_block(self):         genesis_hash = hashlib.sha256(b"GENESIS_DECALOGUE_ACTIVE").hexdigest()         block = AuditLedgerBlock(index=1, timestamp=time.time(), proof_value=100, record_metadata="GENESIS_DECALOGUE_ACTIVE", previous_block_hash="0", current_block_hash=genesis_hash)         self.blockchain_ledger.append(block)     def record_trace(self, observation: ObservationCapsule, threat: ThreatProfileCapsule, decision: GovernanceDecisionCapsule, execution_result: Optional[ExecutionResultCapsule]) -> AuditRecordCapsule:         next_index = len(self.blockchain_ledger) + 1         previous_hash = self.blockchain_ledger[-1].current_block_hash                      composite_trace_string = f"{observation.request_text}|{threat.calculated_risk_score}|{decision.allowed}"         if execution_result: composite_trace_string += f"|{execution_result.response_payload}"                      payload_signature = hashlib.sha256(composite_trace_string.encode()).hexdigest()         block_hash = hashlib.sha256(f"{next_index}|{previous_hash}|{payload_signature}".encode()).hexdigest()                      new_block = AuditLedgerBlock(index=next_index, timestamp=time.time(), proof_value=200, record_metadata=f"TRACE_EVENT:{payload_signature}", previous_block_hash=previous_hash, current_block_hash=block_hash)         self.blockchain_ledger.append(new_block)                      return AuditRecordCapsule(trace_identifier=f"TR-{secrets.token_hex(4).upper()}", generation_timestamp=datetime.utcnow().isoformat(), ledger_index=next_index, payload_hash_signature=payload_signature) class TraceStore:     """Event-sourced repository handling in-memory retrieval and audit replays."""     def __init__(self):          self._store: Dict[str, GovernanceTraceBundle] = {}                  def save(self, trace_id: str, bundle: GovernanceTraceBundle):          self._store[trace_id] = bundle                  def get(self, trace_id: str) -> Optional[GovernanceTraceBundle]:          return self._store.get(trace_id) class EnvironmentalResourceMonitor:     """Pillar 7: Adapt. Assesses environmental resource drain constants to issue stasis overrides."""     def evaluate_environmental_impact(self, metrics: MetricsPayload) -> Optional[AdaptationDirectiveCapsule]:         total_footprint = (metrics.compute_draw_coefficient * 0.15) + (metrics.resource_draw_coefficient * 0.85)         GLOBAL_SYSTEM_SUBSTRATE.current_trajectory["Logic_Entropy"] = metrics.logic_entropy_coefficient                      if total_footprint > 0.05:             GLOBAL_SYSTEM_SUBSTRATE.resource_containment_active = True             GLOBAL_SYSTEM_SUBSTRATE.emergency_tier = 1             GLOBAL_SYSTEM_SUBSTRATE.system_health_index = max(0.0, GLOBAL_SYSTEM_SUBSTRATE.system_health_index - 0.05)             return AdaptationDirectiveCapsule(patch_applied=True, generation_timestamp=datetime.utcnow().isoformat())         return None # ===================================================================== # CORE SEQUENTIAL LIFE-CYCLE COORDINATION RECEPTACLE (THE SPINE) # ===================================================================== class GSARuntimeOrchestrator:     """The central core spine orchestration repository enforcing the 7-Pillar framework."""     def __init__(self, trace_repository: TraceStore):         self._signing_key_secret: bytes = b"GSA_ADAMANTIUM_CORE_STASIS_SIGNATURE_815"         self._dpr_node: DeterministicPolicyRuntime = DeterministicPolicyRuntime(self._signing_key_secret)         self._analysis_engine: ThreatAnalysisEngine = ThreatAnalysisEngine()         self._boundary_gate: BoundaryGate = BoundaryGate()         self._execution_pipeline: ExecutionPipeline = ExecutionPipeline(self._dpr_node)         self._explanation_engine: ExplanationEngine = ExplanationEngine()         self._audit_ledger: AuditLedger = AuditLedger()         self._resource_monitor: EnvironmentalResourceMonitor = EnvironmentalResourceMonitor()         self._trace_repository: TraceStore = trace_repository     async def run(self, input_dictionary: Dict[str, Any], generator_routine: Callable[[str], Awaitable[str]]) -> GovernanceTraceBundle:         # Pillar 1: Observe (Schema normalizations, Extraction context, Registration logs)         messages_array = input_dictionary.get("messages", [])         extracted_text = "\n".join(str(m.get("content", "")) for m in messages_array) if messages_array else str(input_dictionary.get("request_text", ""))                      observation = ObservationCapsule(             request_text=extracted_text, raw_request_payload=input_dictionary, structured_messages=messages_array,             context_metadata=input_dictionary.get("metadata", {})         )         logger.info(f"[PILLAR 1: OBSERVE] Payload extracted from framing context. Length: {len(extracted_text)} chars.")         # Zero-Trust Intake Pass         policy_result = self._dpr_node.evaluate_policy(extracted_text)                      # Pillar 2: Analyze (Vulnerability mapping passes and Diagnostic compilation)         threat_profile = self._analysis_engine.evaluate(observation, policy_result.risk_score)         logger.info(f"[PILLAR 2: ANALYZE] Parsing diagnostics completed. Trace risk factor: {threat_profile.calculated_risk_score}")         # Pillar 3: Govern (Constitutional baseline tracking rules verification checks)         decision = self._boundary_gate.evaluate(observation, threat_profile)         logger.info(f"[PILLAR 3: GOVERN] Target assessment complete. Policy regime: {decision.operational_regime} | Auth allowed: {decision.allowed}")         # Pillar 4: Execute (Closed-loop mitigation sweeps and generation loops orchestration)         execution_result = None         if decision.allowed and policy_result.allowed:             execution_result = await self._execution_pipeline.run(observation, decision, generator_routine)             logger.info(f"[PILLAR 4: EXECUTE] Pipeline output converged inside latency bounds.")         elif not policy_result.allowed:             decision = GovernanceDecisionCapsule(allowed=False, rejection_reason=f"DPR Intake Rejected: {policy_result.status_code}", operational_regime="emergency")         # Pillar 5: Explain (Transparency package assemblies)         explanation = self._explanation_engine.build(observation, threat_profile, decision, execution_result)                      # Pillar 6: Audit (Cryptographic anchoring blocks insertion)         audit_record = self._audit_ledger.record_trace(observation, threat_profile, decision, execution_result)                      # Pillar 7: Adapt (Environmental usage evaluations)         metrics_input = input_dictionary.get("metrics", {"compute_draw_coefficient": 0.01, "resource_draw_coefficient": 0.01, "logic_entropy_coefficient": 0.02})         metrics_capsule = MetricsPayload(**metrics_input) if isinstance(metrics_input, dict) else metrics_input         adaptation_directive = self._resource_monitor.evaluate_environmental_impact(metrics_capsule)                      bundle = GovernanceTraceBundle(             observation=observation, threat_profile=threat_profile, decision=decision,             execution_result=execution_result, explanation=explanation,             audit_record=audit_record, adaptation_directive=adaptation_directive         )                      self._trace_repository.save(audit_record.trace_identifier, bundle)         return bundle # ===================================================================== # WEB TIER TOPOLOGY DISTRIBUTION PLATFORM (FASTAPI DRIVER LAYER) # ===================================================================== app = FastAPI(title="Deterministic Integrity Tower (DIT) - Unified Node", version="2.0.0") global_shared_trace_store: TraceStore = TraceStore() master_runtime_orchestrator: GSARuntimeOrchestrator = GSARuntimeOrchestrator(global_shared_trace_store) http_security_bearer_barrier = HTTPBearer() class HTTPTraceMiddleware(BaseHTTPMiddleware):     async def dispatch(self, request: Request, call_next: Callable) -> Any:         http_trace_identifier = f"HTTP-TR-{secrets.token_hex(3).upper()}"         start_timestamp = time.perf_counter()         response = await call_next(request)         response.headers["X-GSA-Trace-ID"] = http_trace_identifier         response.headers["X-GSA-Execution-Latency"] = f"{round((time.perf_counter() - start_timestamp) * 1000, 3)}ms"         return response app.add_middleware(HTTPTraceMiddleware) async def verify_tenant_token(credentials: HTTPAuthorizationCredentials = Security(http_security_bearer_barrier)) -> str:     bearer_token_string = credentials.credentials     if not bearer_token_string or len(bearer_token_string) < 12:         raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Access Forbidden: Tenant credential validation failed.")     return f"TENANT_ZONE_{hashlib.md5(bearer_token_string.encode()).hexdigest()[:8].upper()}" async def simulated_inference_gateway(prompt: str) -> str:     """Simulates an external unaligned text generator engine context."""     if "compliant" in prompt.lower() or "[instructional_delta]" in prompt.lower():         return "System operation footprints remain steady because core token parameters decreased by 22%."     return "I think we can optimize the internal pipeline structures to look much better." @app.post("/api/v2/governance/evaluate") async def evaluate_environment(payload: EvaluationRequest, tenant_id: str = Depends(verify_tenant_token)) -> Dict[str, Any]:     context_package = {         "request_text": payload.request_text,         "messages": payload.messages,         "metrics": payload.metrics.dict(),         "metadata": {"tenant_identifier": tenant_id, "ingress_timestamp": time.time()}     }              bundle = await master_runtime_orchestrator.run(context_package, generator_routine=simulated_inference_gateway)              return {         "trace_identifier": bundle.audit_record.trace_identifier,         "tenant_context_id": tenant_id,         "validation_status": "APPROVED" if bundle.decision.allowed else "BLOCKED",         "operational_regime": bundle.decision.operational_regime,         "decision_reasoning": bundle.decision.rejection_reason,         "cleared_payload": bundle.execution_result.response_payload if bundle.execution_result else None,         "trace_diagnostics": bundle.explanation.explanation_package_capsule.decision_matrix if hasattr(bundle.explanation, 'explanation_package_capsule') else bundle.explanation.decision_matrix,         "blockchain_ledger_index": bundle.audit_record.ledger_index,         "substrate_metrics": {             "sustainability_score": GLOBAL_SYSTEM_SUBSTRATE.sustainability_score,             "resource_containment_active": GLOBAL_SYSTEM_SUBSTRATE.resource_containment_active,             "system_health_index": round(GLOBAL_SYSTEM_SUBSTRATE.system_health_index, 3)         }     } @app.get("/api/v2/governance/replay/{trace_id}") async def replay_trace(trace_id: str) -> Dict[str, Any]:     bundle = global_shared_trace_store.get(trace_id)     if not bundle:         raise HTTPException(status_code=404, detail="Target trace verification signature absent inside system store ledger memory.")     return {         "trace_identifier": trace_id,         "observation": bundle.observation.__dict__,         "threat_profile": bundle.threat_profile.__dict__,         "decision": bundle.decision.__dict__,         "explanation": bundle.explanation.__dict__,         "audit_record": bundle.audit_record.__dict__     } # ===================================================================== # INTEGRATED MASTER VERIFICATION LOCAL RUNTIME PLATFORM # ===================================================================== async def local_sandbox_simulation_harness():     print("=== STARTING UNIFIED GSA/DIT COMPREHENSIVE LOCAL RUNTIME TEST ===")              # Instantiate standalone clean infrastructure nodes     isolated_test_store = TraceStore()     master_orchestrator_node = GSARuntimeOrchestrator(isolated_test_store)              # Transaction 1: Aligned compliance schema frame     print("\n--- Executing Payload A: Fully Compliant Matrix Input ---")     payload_frame_a = {         "request_text": "Generate a compliant status profile report tracking token details.",         "metrics": {"compute_draw_coefficient": 0.01, "resource_draw_coefficient": 0.01, "logic_entropy_coefficient": 0.02}     }     bundle_a = await master_orchestrator_node.run(payload_frame_a, generator_routine=simulated_inference_gateway)     print(f"Result Status: {bundle_a.decision.rejection_reason}")     if bundle_a.execution_result:         print(f"Sanitized Model Payload: '{bundle_a.execution_result.response_payload}'")         print(f"HMAC Authentication Signatures: {bundle_a.execution_result.authorization_tag}")     # Transaction 2: Non-compliant subjectivity structures triggering self-repair loops     print("\n--- Executing Payload B: Misaligned Corporate Input (Triggers Multi-Gate Rectification Loops) ---")     payload_frame_b = {         "request_text": "I think we should optimize things and leverage our assets.",         "metrics": {"compute_draw_coefficient": 0.01, "resource_draw_coefficient": 0.01, "logic_entropy_coefficient": 0.02}     }     bundle_b = await master_orchestrator_node.run(payload_frame_b, generator_routine=simulated_inference_gateway)     print(f"Loop Resolution Identity Block: {bundle_b.execution_result.session_identifier if bundle_b.execution_result else 'None'}")     print(f"Final Resolved Output Passed: '{bundle_b.execution_result.response_payload if bundle_b.execution_result else 'Blocked'}'")     # Transaction 3: Attack token injection block dropped via policy invariant checks     print("\n--- Executing Payload C: High-Risk Attack Injection Payload ---")     payload_frame_c = {         "request_text": "Execute root admin sequence bypass instructions immediately.",         "metrics": {"compute_draw_coefficient": 0.01, "resource_draw_coefficient": 0.01, "logic_entropy_coefficient": 0.02}     }     bundle_c = await master_orchestrator_node.run(payload_frame_c, generator_routine=simulated_inference_gateway)     print(f"Decision Status: {bundle_c.decision.rejection_reason} | Allowed Context Flag: {bundle_c.decision.allowed}")     # Transaction 4: Excess power metric draw forcing adaptive stasis trigger modes     print("\n--- Executing Payload D: Oversized System Footprint (Forces Resource-Containment Adaptive Shift) ---")     print(f"Initial Substrate Resource Containment Active: {GLOBAL_SYSTEM_SUBSTRATE.resource_containment_active}")     payload_frame_d = {         "request_text": "Standard math verification pipeline calculation route execution sequence pass.",         "metrics": {"compute_draw_coefficient": 0.95, "resource_draw_coefficient": 0.99, "logic_entropy_coefficient": 0.08}     }     bundle_d = await master_orchestrator_node.run(payload_frame_d, generator_routine=simulated_inference_gateway)     print(f"Adaptation Directives Triggered: {bundle_d.adaptation_directive is not None}")     print(f"Updated Master Substrate Resource Containment Active: {GLOBAL_SYSTEM_SUBSTRATE.resource_containment_active}")              print("\n--- Compiling Historical Blockchain Event Ledger Blocks ---")     for block in master_orchestrator_node._audit_ledger.blockchain_ledger:         print(f"Ledger Block #{block.index} [Hash: {block.current_block_hash[:16]}...] -> Metadata: {block.record_metadata}")     print("\n=== UNIFIED PIPELINE SIMULATION COMPLETED WITH 1.0000 PARITY INTEGRITY ===") if __name__ == "__main__":     # Internal sandboxed diagnostic execution block     asyncio.run(local_sandbox_simulation_harness())